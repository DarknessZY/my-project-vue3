import { Options } from './types';
export declare type LockOptions = Options;
export default class Lock {
    private opt;
    on: boolean;
    constructor(opt: Options);
    getValue(): Promise<any>;
    setValue(value: any): Promise<void>;
    clear(): Promise<void>;
    resolvePromise: any;
    promise: Promise<any>;
    static createLockWrapper(lock: Lock): (fn: () => Promise<any>) => Promise<any>;
}
