import { Adapter, Config, PQRequest, PQResponse, PreQuestInstance, Context, MiddlewareInjectOptions } from '@prequest/types';
import { Middleware } from './Middleware';
export declare class PreQuest extends Middleware {
    private adapter;
    private config?;
    constructor(adapter: Adapter, config?: Partial<PQRequest> | undefined);
    private mount;
    request<Q>(path: string | Config, config?: Config): Promise<PQResponse<Q>>;
    controller<Q>(ctx: Context, opt?: MiddlewareInjectOptions): Promise<PQResponse<Q>>;
    static defaults: Config;
    static create(adapter: Adapter, config?: Config): PreQuestInstance;
}
