import { Config } from '@prequest/types';
interface PQErrorOptions {
    code: ErrorCode;
    config: Config;
}
export declare class PQError extends Error {
    private opt?;
    code?: ErrorCode;
    config?: Config;
    constructor(opt?: Partial<PQErrorOptions> | undefined);
}
export declare enum ErrorCode {
    timeout = "timeout",
    abort = "abort",
    common = "common"
}
export declare function createError(code: ErrorCode, message: string, opt: Config): PQError;
export declare function enhanceError(error: PQError, code: ErrorCode, opt: Config): PQError;
export {};
